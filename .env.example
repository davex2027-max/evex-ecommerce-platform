# Local development (default)
MONGO_URI=mongodb://localhost:27017/evex_db
# Production (MongoDB Atlas) — use on Render, not locally.
# MONGO_URI=mongodb+srv://<db_username>:<db_password>@evexecommerceplatform.jkuvcnm.mongodb.net/evex_db?appName=evexecommerceplatform
# AUTO_SEED=true seeds the database only when it is empty (set on Render)

JWT_SECRET=your_jwt_secret_here
PAYSTACK_SECRET_KEY=sk_live_your_paystack_secret_key
PAYSTACK_CURRENCY=NGN
CORS_ORIGIN=http://localhost:3000
PORT=5000
